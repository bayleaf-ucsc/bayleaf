{
  "id": "lathe",
  "user_id": "5cbb2fa9-feeb-4e6f-9d58-e300f52448f8",
  "name": "Code Sandbox",
  "meta": {
    "description": "Coding agent tools (lathe, bash, read, write, edit, onboard, expose, destroy) backed by per-user sandbox VMs with transparent lifecycle management.",
    "manifest": {
      "title": "Lathe",
      "author": "Adam Smith",
      "author_url": "https://adamsmith.as",
      "description": "Coding agent tools (lathe, bash, read, write, edit, glob, grep, interpret, delegate, onboard, expose, destroy) backed by per-user sandbox VMs with transparent lifecycle management.",
      "required_open_webui_version": "0.4.0",
      "requirements": "httpx, httpx-ws, pydantic-ai-slim[openai]~=1.60, cachetools",
      "version": "0.24.1",
      "licence": "MIT"
    },
    "has_user_valves": true
  },
  "access_grants": [
    {
      "id": "363080fd-5cad-4784-a2c1-afe7f0e75c6e",
      "resource_type": "tool",
      "resource_id": "lathe",
      "principal_type": "user",
      "principal_id": "*",
      "permission": "read",
      "created_at": 1773097882
    }
  ],
  "updated_at": 1784225071,
  "created_at": 1773079252,
  "user": null,
  "write_access": true,
  "specs": [
    {
      "name": "bash",
      "description": "Execute a bash command in the sandbox. Non-interactive only.\nCommands that finish within the foreground window return output directly.\nLong-running commands auto-background and return a descriptor with log\nfile paths for monitoring.\n\nWhen creating git commits, add an Assisted-by: Lathe trailer to the\ncommit message to acknowledge AI assistance.",
      "parameters": {
        "properties": {
          "command": {
            "description": "The bash command to execute.",
            "type": "string"
          },
          "workdir": {
            "default": "/home/daytona/workspace",
            "description": "Working directory (default: /home/daytona/workspace).",
            "type": "string"
          },
          "foreground_seconds": {
            "default": -1,
            "description": "Seconds to wait before auto-backgrounding. Omit or set -1 to use the server default. Set 0 for immediate background (fire-and-forget). Use higher positive values for known-slow commands.",
            "type": "integer"
          }
        },
        "required": [
          "command"
        ],
        "type": "object"
      }
    },
    {
      "name": "delegate",
      "description": "Delegate a multi-step task to an autonomous sub-agent with sandbox access.\nThe sub-agent runs the same model, has bash/read/write/edit/glob/grep tools,\nand returns a summary when done. Use for exploration, refactoring, debugging,\nor any multi-step work that doesn't need user interaction.\nLong-running delegations auto-background and return a descriptor with log/result\nfile paths for monitoring, exactly like bash() does for long commands.",
      "parameters": {
        "properties": {
          "task": {
            "description": "What the sub-agent should accomplish. Be specific — it cannot ask clarifying questions. Include any context (error messages, prior findings, instructions) directly in the task description.",
            "type": "string"
          },
          "context_files": {
            "default": [],
            "description": "Absolute sandbox file paths to inject into the sub-agent's prompt (e.g. AGENTS.md, SKILL.md, config files). Fetched at delegation time — the sub-agent sees their contents without spending steps reading them.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "max_steps": {
            "default": 10,
            "description": "Maximum inference calls the sub-agent may make (default: 10, max: 30).",
            "type": "integer"
          },
          "foreground_seconds": {
            "default": -1,
            "description": "Seconds to wait before auto-backgrounding (default: 30, max: 300). Set 0 for immediate background (fire-and-forget). Omit or set -1 to use the default.",
            "type": "integer"
          }
        },
        "required": [
          "task"
        ],
        "type": "object"
      }
    },
    {
      "name": "destroy",
      "description": "Permanently destroy the sandbox VM. Irreversible.\nPrompts the user for confirmation before proceeding.\nIf a persistent volume is attached, its data is preserved and will reappear in the next sandbox.",
      "parameters": {
        "properties": {},
        "type": "object"
      }
    },
    {
      "name": "edit",
      "description": "Edit a file by exact string replacement. Fails on ambiguous matches unless replace_all=true.",
      "parameters": {
        "properties": {
          "path": {
            "description": "Absolute path to the file.",
            "type": "string"
          },
          "old_string": {
            "description": "Exact text to find.",
            "type": "string"
          },
          "new_string": {
            "description": "Replacement text.",
            "type": "string"
          },
          "replace_all": {
            "default": false,
            "description": "Replace all occurrences (default: false).",
            "type": "boolean"
          }
        },
        "required": [
          "path",
          "old_string",
          "new_string"
        ],
        "type": "object"
      }
    },
    {
      "name": "expose",
      "description": "Expose a sandbox service to the user. Pass \"dufs\" for a one-step file\nbrowser, \"code-server\" for a one-step IDE, \"http:<port>\" for a web\nserver you already started, or \"ssh\" for interactive shell access.",
      "parameters": {
        "properties": {
          "target": {
            "description": "What to expose — \"dufs\" for file upload/download, \"code-server\" for a browser IDE, \"ssh\" for a shell, or \"http:<port>\" (e.g. \"http:5000\", port range 3000–9999) for an HTTP service you started manually.",
            "type": "string"
          }
        },
        "required": [
          "target"
        ],
        "type": "object"
      }
    },
    {
      "name": "glob",
      "description": "Search for files in the sandbox workspace by glob pattern.\nReturns a hierarchical listing of matches with absolute paths.\nDense directories are collapsed with match counts; narrow the\npattern or increase max_lines to expand them.",
      "parameters": {
        "properties": {
          "pattern": {
            "description": "Comma-separated globs, !-prefix to exclude. Examples: '**/*.py', 'src/**/*.ts,!**/node_modules/**'.",
            "type": "string"
          },
          "max_lines": {
            "default": 100,
            "description": "Max output lines (default: 100).",
            "type": "integer"
          }
        },
        "required": [
          "pattern"
        ],
        "type": "object"
      }
    },
    {
      "name": "grep",
      "description": "Search file contents in the sandbox workspace by regex.\nReturns matches grouped by file with line numbers. Dense\ndirectories and files are collapsed with match counts;\nnarrow the file scope or increase max_lines to expand them.",
      "parameters": {
        "properties": {
          "pattern": {
            "description": "Regex to search for (e.g. 'import.*asyncio', 'TODO|FIXME').",
            "type": "string"
          },
          "files": {
            "default": "**/*",
            "description": "File scope as comma-separated globs (default: '**/*'). !-prefix to exclude.",
            "type": "string"
          },
          "max_lines": {
            "default": 100,
            "description": "Max output lines (default: 100).",
            "type": "integer"
          }
        },
        "required": [
          "pattern"
        ],
        "type": "object"
      }
    },
    {
      "name": "handoff",
      "description": "Prepare a handoff document for continuing this work in a new conversation. Call this when the user asks to compact, hand off, or save context for a fresh chat. After calling, write the handoff as your reply — do not make any more tool calls.",
      "parameters": {
        "properties": {},
        "type": "object"
      }
    },
    {
      "name": "interpret",
      "description": "Run Python code in a persistent REPL session. Variables, imports, and\ndefinitions survive across calls within the same conversation.\nUse this for iterative data exploration, incremental computation, or\nany workflow where you need state to persist between executions.\nFor one-shot shell commands or non-Python work, prefer bash() instead.",
      "parameters": {
        "properties": {
          "code": {
            "description": "Python code to execute. Top-level await is not supported.",
            "type": "string"
          },
          "timeout": {
            "default": 120,
            "description": "Max execution time in seconds (default: 120). Use 0 for no limit.",
            "type": "integer"
          }
        },
        "required": [
          "code"
        ],
        "type": "object"
      }
    },
    {
      "name": "lathe",
      "description": "Manual for the lathe toolkit. Call lathe(manpage=\"overview\") before your first tool use in a new conversation to learn the sandbox model, available workflows, and gotchas. Costs one tool call, saves many.",
      "parameters": {
        "properties": {
          "manpage": {
            "default": "overview",
            "description": "Which manual page to return. Use \"overview\" for big-picture orientation, \"version\" for the installed version.",
            "type": "string"
          }
        },
        "type": "object"
      }
    },
    {
      "name": "onboard",
      "description": "Load project context (directory listing, AGENTS.md, skill catalog) at the start of a conversation.\nAlways returns a directory listing of the target path. Additionally\nsearches the project directory and ~/.agents/ for agent instructions and skills.\nUse read() on a skill's SKILL.md path to load its full instructions later.",
      "parameters": {
        "properties": {
          "path": {
            "description": "Absolute path to the project root (e.g. /home/daytona/workspace/myproject).",
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      }
    },
    {
      "name": "read",
      "description": "Read a file from the sandbox. Returns numbered lines.\nSupports Python-style negative indexing: start=-10 reads from\n10 lines before the end, stop=-3 stops 3 lines before the end.\nAll positive values are 1-indexed. stop is exclusive (half-open).\nstop=0 (default) means end of file.",
      "parameters": {
        "properties": {
          "path": {
            "description": "Absolute path to the file.",
            "type": "string"
          },
          "start": {
            "default": 1,
            "description": "First line to return (1-indexed). Negative counts from end. 0 is treated as 1.",
            "type": "integer"
          },
          "stop": {
            "default": 0,
            "description": "Line to stop before (exclusive, 1-indexed). Negative counts from end. 0 means end of file.",
            "type": "integer"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      }
    },
    {
      "name": "write",
      "description": "Write a file to the sandbox (creates parents automatically).\n\nFor large files, prefer a skeleton-then-edit workflow: write the file first\nwith overall structure and ``# PLACEHOLDER: <section>`` markers, then use\n``edit()`` to expand each placeholder incrementally. This makes progress\nvisible to the user in real time and keeps partial work on disk if\ngeneration is interrupted.",
      "parameters": {
        "properties": {
          "path": {
            "description": "Absolute path to write to.",
            "type": "string"
          },
          "content": {
            "description": "The full file content.",
            "type": "string"
          }
        },
        "required": [
          "path",
          "content"
        ],
        "type": "object"
      }
    }
  ]
}
